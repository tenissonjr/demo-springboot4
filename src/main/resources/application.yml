spring:
  application:
    name: demo-springboot4
  validation:
    use-lombok-nonnull: true
ibge:
  base:
    url: https://servicodados.ibge.gov.br
bcnpj:
  base:
    url: ${env:bcnpj_url}
  credentials:
    username: ${env:bcnpj_username}
    password: ${env:bcnpj_password}


